Your workflow from now on:-
When your friend pushes to the main repository:
  git fetch upstream
  git merge upstream/main
  git push origin main

When you make your own changes:
  git add .
  git commit -m "Your message"
  git push origin main