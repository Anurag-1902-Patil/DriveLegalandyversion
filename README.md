# DriveLegal

DriveLegal is a local-first FastAPI and HTML/Streamlit application for authenticated Indian traffic-law Q&A, GPS-aware retrieval, challan estimation, and driver dashboard history.

## 1. Project Title & One-Line Description

DriveLegal is a location-aware road-safety assistant for Indian drivers that combines a FastAPI backend, local Qdrant retrieval, Ollama/Mistral generation, SQLite persistence, and an HTML dashboard.

## 2. Overview

DriveLegal answers natural-language traffic-law questions and returns indicative, source-backed guidance for a selected or GPS-resolved Indian jurisdiction. The main user experience is `frontend/ui.html`, a static HTML/CSS/JavaScript dashboard embedded by `frontend/app.py` in Streamlit and also served directly by FastAPI at `/ui`.

The backend is a single FastAPI service (`app/main.py`) with routers for email OTP authentication, chat, GPS location updates, and profile/dashboard data. It stores users, violations, and chat messages in the root SQLite database configured by `app/database.py` as `sqlite:///./drivelegal.db`.

**Jurisdiction Hierarchy**: DriveLegal now uses a structured hierarchy of Indian jurisdictions (National → State → City → Local) as data structures. This hierarchy enables more precise location-aware retrieval and filtering of traffic laws and regulations. The system automatically resolves GPS coordinates to the appropriate jurisdiction level and retrieves location-specific content through a cascading filter mechanism that prioritizes more specific jurisdictions over broader ones.

The AI path is local-first. `rag/retriever.py` loads a local embedded Qdrant collection from `data/qdrant_db`, embeds user queries with `sentence-transformers` model `all-MiniLM-L6-v2`, and returns location-filtered law chunks. `rag/chain.py` sends retrieved context to local Ollama model `mistral` through LangChain's `ChatOllama`; if the model call fails, it falls back to direct Qdrant retrieval and then `data/cache.json`.

Structured fine amounts are not generated by the LLM. `rag/challan.py` detects violation and vehicle keywords, then looks up exact strings in `data/fines.json` using city -> state -> national priority.

## 3. System Architecture

This is a local client-server application with an offline-capable RAG/indexing pipeline. It is not implemented as microservices: one FastAPI process hosts all API routes, startup initialization, database table creation, and Qdrant index loading. A separate Streamlit process can host the UI wrapper, but the browser JavaScript calls the FastAPI backend directly.
![System Architecture](https://github.com/sohmh/DriveLegal/blob/main/drivelegal-system-architecture.png)

Major components:

| Component | Responsibility |
| --- | --- |
| `frontend/ui.html` | Main browser dashboard: OTP login, dashboard/profile panels, chat UI, manual jurisdiction selector, local challan calculator, GPS toggle, theme toggle, and browser voice input. |
| `frontend/app.py` | Streamlit wrapper that reads `frontend/ui.html` and renders it via `streamlit.components.v1.components.html`. It does not proxy API requests. |
| `app/main.py` | FastAPI entrypoint. Loads `.env`, configures CORS/logging, creates SQLAlchemy tables, loads the Qdrant index on lifespan startup, registers routers, serves `/ui`, and exposes `/health`. |
| `app/auth.py` | Email OTP authentication. Generates in-memory OTPs, sends them over SMTP, creates users on first verification, and issues JWTs signed with `JWT_SECRET`. |
| `app/routes/chat.py` | Authenticated `/chat` endpoint. Chooses GPS location if fresh, resolves jurisdiction hierarchy, runs RAG, performs structured fine lookup, computes confidence, writes chat history, and returns `ChatResponse`. |
| `app/routes/location.py` | Accepts browser GPS fixes, resolves them offline with `reverse_geocoder`, maps to jurisdiction hierarchy, and keeps the latest fix in process memory until `GPS_MAX_AGE_SECONDS` expires. |
| `app/routes/profile.py` | Dashboard/profile endpoints. Despite Surepass environment variables, current code hydrates profile and violation data from local default/sample records rather than making an external Surepass HTTP request. TODO: confirm whether live Surepass integration is intended. |
| `rag/retriever.py` | Loads embedded Qdrant plus the sentence-transformer embedder; retrieves chunks by jurisdiction hierarchy filter cascade: city → state → country → unfiltered. Uses the structured hierarchy data to prioritize location-specific content. |
| `rag/chain.py` | Builds the RAG prompt, invokes local Ollama `mistral`, and handles offline fallback through Qdrant/cache. |
| `rag/challan.py` | Deterministic fine lookup using keyword detection and jurisdiction-aware `data/fines.json` structure; this avoids LLM-generated fine amounts. |
| `geocoding/offline_geocoder.py` | Resolves GPS coordinates to offline geocoded addresses and maps them to the jurisdiction hierarchy (city, state, country). Provides structured jurisdiction data for cascading retrieval. |
| `scripts/preprocess.py` | Reads `.txt`, `.md`, `.pdf`, and `.csv` files from `data/raw`, extracts/cleans text, chunks it, infers metadata including jurisdiction hierarchy levels, and writes `data/processed/chunks.json`. |
| `scripts/migrate_to_qdrant.py` | Embeds processed chunks and recreates the embedded Qdrant collection `drivelegal_chunks` under `data/qdrant_db`. Indexes chunks with jurisdiction hierarchy metadata for filtered retrieval. |

## 4. Use-Case Workflow

### Email OTP login and dashboard bootstrap

1. The browser loads `frontend/ui.html` through Streamlit (`streamlit run frontend/app.py`) or FastAPI (`GET /ui`).
2. The user enters an email address.
3. The UI posts `{ "email": "..." }` to `POST /api/auth/send-otp`.
4. `app/auth.py` generates a six-digit OTP, stores it in the module-level `otp_store`, and sends the code through `smtplib.SMTP_SSL`.
5. The user enters the OTP.
6. The UI posts `{ "email": "...", "code": "..." }` to `POST /api/auth/verify-otp`.
7. The backend validates the in-memory OTP, creates a `users` row if needed, signs a JWT with `JWT_SECRET`, and returns `token`, `is_verified`, and `user_id`.
8. The UI stores the token in `localStorage` under `drivelegal_jwt`, then calls `/api/profile/dashboard-data/{user_id}` and `/api/profile/chat-history/{user_id}`.

### Primary chat workflow
![Use-Case Workflow](https://github.com/sohmh/DriveLegal/blob/main/drivelegal-primarychatworkflow.png)


### GPS workflow

1. The user enables GPS in the right-side dashboard panel.
2. Browser `navigator.geolocation.watchPosition` collects coordinates.
3. The UI posts `{ lat, lon, accuracy, client_timestamp }` to `POST /location/update` immediately and every 10 seconds while a fix exists.
4. `app/routes/location.py` resolves coordinates with `geocoding/offline_geocoder.py` and maps them to the jurisdiction hierarchy (city, state, country).
5. The latest resolved `LocationResult` with jurisdiction hierarchy data stays only in process memory.
6. `/chat` uses that GPS location and its corresponding jurisdiction level instead of the manual dropdown while the fix is fresh, enabling precise jurisdiction-aware content retrieval.

### Driver profile verification workflow

1. After login, if `profile.is_verified` is false, the UI shows a one-time license-linking form.
2. The UI posts email, DL number, and DOB to `POST /api/profile/verify-dl`.
3. `app/routes/profile.py` finds the existing user by email.
4. Current code checks whether the DL number contains `ANURAG` or `TEAMKALKI`; if so, it uses the matching sample profile. Otherwise it uses a default profile and violation.
5. The route marks the user verified, saves DL/profile fields, clears existing violations for the user, inserts selected violations, and returns profile details.
6. The UI reloads dashboard and chat history.

### Corpus ingestion and indexing workflow

1. Add source files to `data/raw` using supported extensions: `.txt`, `.md`, `.pdf`, `.csv`.
2. Run `python scripts/preprocess.py`.
3. The script extracts text, strips non-ASCII characters, chunks text with `RecursiveCharacterTextSplitter`, adds inferred `source`, `filetype`, `chunk_index`, `region`, `category`, `law_section`, and jurisdiction hierarchy metadata, and writes `data/processed/chunks.json`.
4. Run `python scripts/migrate_to_qdrant.py`.
5. The script loads `data/processed/chunks.json`, generates normalized 384-dimensional embeddings with `all-MiniLM-L6-v2`, recreates Qdrant collection `drivelegal_chunks` with jurisdiction hierarchy indexed for filtered retrieval, and writes local embedded Qdrant storage under `data/qdrant_db`.

## 5. Tech Stack

| Category | Technology | Version/source | Why it is used here |
| --- | --- | --- | --- |
| Languages | Python | `.python-version` = `3.11`, `runtime.txt` = `python-3.11` | Backend, RAG pipeline, preprocessing scripts, tests, and Streamlit wrapper. |
| Languages | HTML/CSS/JavaScript | `frontend/ui.html` | Main dashboard UI, browser API calls, local challan calculator, GPS tracking, and voice input. |
| Backend/framework | FastAPI | `fastapi==0.111.0` | Hosts `/chat`, auth, location, profile, `/ui`, and `/health`. |
| Backend/framework | Uvicorn | `uvicorn[standard]==0.29.0` | ASGI development server used by `start.bat` and setup docs. |
| Backend/framework | Pydantic | `pydantic==2.7.0` | Validates request/response models in `app/models.py`, `app/auth.py`, and route-local schemas. |
| Backend/framework | SQLAlchemy | `SQLAlchemy==2.0.30` | Defines and creates SQLite tables for users, violations, and chat history. |
| Auth | PyJWT | `PyJWT==2.8.0` | Signs and verifies bearer JWTs after OTP verification. |
| Auth | email-validator | `email-validator==2.1.1` | Required by Pydantic `EmailStr` in auth payloads. |
| Config | python-dotenv | `python-dotenv==1.0.1` | Loads `.env` in `app/main.py` and `scripts/cache_rules.py`. |
| AI/RAG | LangChain | `langchain==0.2.1` | Builds the prompt template and uses LangChain document types. |
| AI/RAG | langchain-community | `langchain-community==0.2.1` | Provides `ChatOllama` for local Mistral calls. |
| AI/RAG | Ollama | External local runtime | Runs the hardcoded `mistral` model used by `rag/chain.py`. |
| AI/RAG | qdrant-client | `qdrant-client==1.18.0` | Embedded local vector database at `data/qdrant_db`. |
| AI/RAG | sentence-transformers | `sentence-transformers==3.0.1` | Loads `all-MiniLM-L6-v2` for chunk and query embeddings. |
| Data processing | pdfplumber | Imported in `scripts/preprocess.py`; not listed in `requirements.txt` | Extracts text from PDF law sources. TODO: confirm/pin version in requirements. |
| Data processing | Python `csv`/`json` | Standard library | Reads CSV law/challan datasets and writes processed JSON chunks/cache. |
| Frontend | Streamlit | `streamlit==1.35.0` | Serves the HTML dashboard through a Streamlit shell. |
| Frontend | Browser `fetch` API | `frontend/ui.html` | Calls FastAPI endpoints directly from the dashboard. |
| Frontend | Google Fonts CDN | `frontend/ui.html` | Loads DM Sans and Playfair Display font CSS. |
| Frontend | Font Awesome CDN | `frontend/ui.html` | Provides UI icons. |
| Browser APIs | Geolocation API | `navigator.geolocation.watchPosition` | Sends live coordinates to `/location/update`. |
| Browser APIs | Web Speech API | `SpeechRecognition` / `webkitSpeechRecognition` | Optional voice transcription in Chrome/Edge-like browsers. |
| Database/storage | SQLite | `drivelegal.db`, SQLAlchemy engine | Persistent user, violation, and chat history storage. |
| Database/storage | Qdrant embedded SQLite | `data/qdrant_db/collection/drivelegal_chunks/storage.sqlite` | Local vector store for 30,633 processed chunks. |
| Database/storage | JSON files | `data/fines.json`, `data/cache.json`, `data/processed/chunks.json` | Fine lookup, offline fallback answers, and processed retrieval corpus. |
| External service | SMTP | `smtplib.SMTP_SSL` | Sends OTP email through configured SMTP server. |
| External service | Surepass config | `SUREPASS_API_BASE_URL`, `SUREPASS_AUTH_TOKEN` | Variables are read in `profile.py`, but no live API call exists in current code. |
| Testing | pytest | `pytest==8.2.1` | Unit/integration test runner. |
| Testing | pytest-asyncio | `pytest-asyncio==0.23.7` | Declared for async tests, though current tests do not use async fixtures directly. |
| Testing | httpx | `httpx==0.27.0` | Supports FastAPI `TestClient`. |
| DevOps/scripts | Windows batch files | `setup.bat`, `start.bat`, `scripts/setup_gps.bat` | Local Windows setup/start helpers. |
| DevOps/deploy hint | `runtime.txt` | `python-3.11` | Python runtime hint for platforms that read `runtime.txt`. |

## 6. How the Tech Stack Works Together

`app/main.py` is the backend entrypoint. On startup it loads `.env`, creates SQLite tables from SQLAlchemy models, and calls `rag.retriever.load_index()`. The app will fail startup if Qdrant collection `drivelegal_chunks` is not available at `data/qdrant_db`; the error message tells the operator to run `python scripts/migrate_to_qdrant.py`.

The browser UI uses a hardcoded backend origin, `http://127.0.0.1:8000`, in `frontend/ui.html`. `frontend/app.py` does not read `BACKEND_URL`; it only embeds the static HTML in Streamlit. If the API runs somewhere else, `frontend/ui.html` must be changed or made configurable.

Authentication is split between browser storage and backend JWT validation. The UI stores the token in `localStorage` and sends it as `Authorization: Bearer <token>` to `/chat`. FastAPI's `HTTPBearer` dependency makes `/chat` require a bearer token before route logic runs. Dashboard/profile endpoints accept `user_id` path parameters and do not currently require the JWT.

**Jurisdiction Hierarchy Resolution**: The GPS and location workflow now integrates jurisdiction hierarchy resolution. When a user enables GPS or manually selects a location, the system resolves it to a structured jurisdiction hierarchy (city → state → country). This hierarchy is passed through the retrieval pipeline to enable cascading, jurisdiction-aware chunk filtering. The RAG system retrieves content in order of jurisdiction specificity: first from the city level, then state, then country/national, and finally from all chunks if needed.

The RAG runtime depends on three local services/artifacts being ready: the FastAPI process, the embedded Qdrant collection under `data/qdrant_db` (indexed with jurisdiction hierarchy metadata), and an Ollama daemon with the `mistral` model available. Qdrant and the sentence-transformer model are loaded in-process; Ollama is contacted through LangChain's `ChatOllama`.

The ingestion pipeline is offline after dependencies/models are installed. `scripts/preprocess.py` creates `data/processed/chunks.json` with jurisdiction hierarchy metadata inferred from content and source attribution; `scripts/migrate_to_qdrant.py` turns that JSON into the Qdrant collection indexed for jurisdiction-specific retrieval. `rag/retriever.py` then reads only the Qdrant store at runtime, not the raw PDFs/CSVs.

Structured fine lookup runs alongside RAG. `/chat` always calls `lookup_fine()` after `get_answer()`. This can attach a `fine_amount` from `data/fines.json` (which is now jurisdiction-aware) even when the generated answer comes from the LLM or fallback path.

SQLite is used directly in the FastAPI process through SQLAlchemy sessions. The configured database file is `./drivelegal.db` relative to the process working directory. A second `app/drivelegal.db` file exists in the repository, but the current `DATABASE_URL` does not point to it.

## 7. Repository Structure & File-by-File Breakdown

Generated/dependency folders such as `.git`, `venv`, `__pycache__`, `.pytest_cache`, and generated Qdrant internals are excluded from this tree. Runtime data artifacts are shown where they affect behavior.

```text
DriveLegal/
|-- .env
|-- .env.example
|-- .gitattributes
|-- .gitignore
|-- .python-version
|-- DriveLegal_Report.pdf
|-- README.md
|-- app/
|   |-- __init__.py
|   |-- auth.py
|   |-- database.py
|   |-- drivelegal.db
|   |-- main.py
|   |-- models.py
|   `-- routes/
|       |-- __init__.py
|       |-- chat.py
|       |-- location.py
|       `-- profile.py
|-- data/
|   |-- cache.json
|   |-- fines.json
|   |-- processed/
|   |   |-- .gitkeep
|   |   `-- chunks.json
|   |-- qdrant_db/
|   |   |-- .lock
|   |   |-- meta.json
|   |   `-- collection/drivelegal_chunks/storage.sqlite
|   `-- raw/
|       |-- .gitkeep
|       |-- Assam motor vehicle_rules2003_0.pdf
|       |-- Gujarat.pdf
|       |-- Haryana motor.pdf
|       |-- Indian_Traffic_Violations.csv
|       |-- J&K acts, rules and guidelines.pdf
|       |-- J&K motor.pdf
|       |-- Karnataka motor act.pdf
|       |-- Keralal motor act.pdf
|       |-- Maharashtra acts and fines.pdf
|       |-- Maharashtra.pdf
|       |-- Motor Vehicles (Amendment) Act, 2019.pdf
|       |-- Telangana rules_of_the_road_regulations_1989.pdf
|       |-- The_M_P_Motor_Vehicles_Rules_1994.PDF
|       |-- The_Punjab_Motor_Vehicles_Rules_1989.PDF
|       |-- catalogue of irc publications may 201783262ee5-0fb2-44a6-9117-87b51e4509c9.pdf
|       |-- cmvr-1989.pdf
|       |-- delhi-motor-vehicles-rules-1993-as-amended-secretariat-6344833.pdf
|       |-- echallan_daily_data.csv
|       |-- goa- fines.pdf
|       |-- motorvehiclesact1989.pdf
|       |-- mumbai fines (1).pdf
|       |-- mva_helmet.txt
|       |-- ndian Legal Corpus (ILC) A Dataset for A dataset summarizing.pdf
|       `-- tamil nadu motor act.pdf
|-- drivelegal.db
|-- frontend/
|   |-- app.py
|   `-- ui.html
|-- geocoding/
|   |-- __init__.py
|   `-- offline_geocoder.py
|-- github guide.txt
|-- logs/
|   `-- app.log
|-- qdrant_db/
|   |-- .lock
|   `-- meta.json
|-- rag/
|   |-- __init__.py
|   |-- chain.py
|   |-- challan.py
|   `-- retriever.py
|-- requirements.txt
|-- runtime.txt
|-- scripts/
|   |-- cache_rules.py
|   |-- migrate_to_qdrant.py
|   |-- preprocess.py
|   `-- setup_gps.bat
|-- setup.bat
|-- start.bat
|-- startingapp.txt
`-- tests/
    |-- test_api.py
    |-- test_challan.py
    `-- test_rag.py
```

Root files:

| File | Role |
| --- | --- |
| `.env` | Local environment file. It is present in `git ls-files` even though `.gitignore` says `.env` should not be committed; do not publish real secrets from this file. |
| `.env.example` | Template documenting host/port, frontend URL, logging, SMTP/JWT, and Surepass variables. Some variables in it are not consumed by current code. |
| `.gitattributes` | Configures Git LFS filters for binary/model/archive file types such as `.bin`, `.pt`, `.npy`, `.h5`, `.zip`. |
| `.gitignore` | Ignores `venv`, Python caches, `.env`, Qdrant stores, model binaries, logs, and OS/editor files. Some ignored patterns still have tracked files in this checkout. |
| `.python-version` | Declares Python `3.11`. |
| `runtime.txt` | Declares `python-3.11` for deployment platforms that use this convention. |
| `requirements.txt` | Pinned Python dependencies for backend, RAG, frontend, GPS, and tests. It does not include `pdfplumber`, which `scripts/preprocess.py` imports. |
| `DriveLegal_Report.pdf` | 33-page project report describing DriveLegal as a location-aware road-safety chatbot for a 2026 hackathon; useful context, but code is the source of truth for this README. |
| `drivelegal.db` | Configured SQLite database used by `app/database.py` when running from the repo root. Contains `users`, `violations`, and `chat_messages`. |
| `setup.bat` | Windows setup helper. It creates `venv`, installs requirements, copies `.env.example`, preprocesses data, then calls missing `scripts\embed.py`; use `scripts\migrate_to_qdrant.py` manually until fixed. |
| `start.bat` | Windows launcher. Starts Uvicorn on port 8000, starts Streamlit on port 8501, and opens the Streamlit URL. |
| `startingapp.txt` | Manual startup notes for Ollama, Uvicorn, and Streamlit. |
| `github guide.txt` | Short Git workflow note for fetching/merging upstream and pushing origin. |
| `README.md` | This documentation. |

`app/`:

| File | Role |
| --- | --- |
| `app/__init__.py` | Marks `app` as a Python package. |
| `app/main.py` | FastAPI entrypoint and application wiring. Creates log handlers, initializes DB tables and Qdrant on startup, includes routers, serves `/ui`, and exposes `/health`. |
| `app/database.py` | Central SQLAlchemy setup. Hardcodes `DATABASE_URL = "sqlite:///./drivelegal.db"`, engine, `SessionLocal`, `Base`, and `get_db()` dependency. |
| `app/models.py` | Pydantic schemas for chat/location responses and SQLAlchemy models for `users`, `violations`, and `chat_messages`. |
| `app/auth.py` | OTP/JWT auth router at `/api/auth`; sends OTP email via SMTP and stores OTPs in process memory. |
| `app/drivelegal.db` | Secondary SQLite file present under `app/`; current code does not point at it. It has `users` and `violations` tables but no `chat_messages` table in the inspected file. |

`app/routes/`:

| File | Role |
| --- | --- |
| `app/routes/__init__.py` | Marks route package. |
| `app/routes/chat.py` | Defines authenticated `POST /chat`; resolves effective location, calls RAG/fine lookup, computes confidence, stores chat history, and returns `ChatResponse`. |
| `app/routes/location.py` | Defines `POST /location/update` and `GET /location/current`; stores only the latest GPS fix in memory and expires it after `GPS_MAX_AGE_SECONDS`. |
| `app/routes/profile.py` | Defines profile verification, dashboard data, and chat-history endpoints under `/api/profile`; currently uses local sample/default profile data. |

`rag/`:

| File | Role |
| --- | --- |
| `rag/__init__.py` | Marks RAG package. |
| `rag/retriever.py` | Loads Qdrant and `SentenceTransformer`, builds cascading jurisdiction hierarchy filters (city → state → country), retrieves and deduplicates documents, and returns `(documents, top_score)`. Uses structured jurisdiction data to prioritize location-specific legal content. |
| `rag/chain.py` | Defines the system prompt, normalizes fallback queries, loads cache JSON, invokes Ollama/Mistral with jurisdiction-aware context, and handles offline fallback. |
| `rag/challan.py` | Loads `data/fines.json` with jurisdiction-aware structure, detects violation/vehicle keywords, and returns deterministic fine strings prioritized by jurisdiction level. |

`geocoding/`:

| File | Role |
| --- | --- |
| `geocoding/__init__.py` | Marks geocoding package. |
| `geocoding/offline_geocoder.py` | Lazy-loads `reverse_geocoder`, maps country/state names, resolves GPS coordinates to jurisdiction hierarchy levels (city, state, country), returns `LocationResult` with structured jurisdiction data, and includes a `haversine_km` helper. |

`frontend/`:

| File | Role |
| --- | --- |
| `frontend/app.py` | Streamlit page setup and HTML embed. Imports `requests` but current code does not call it. |
| `frontend/ui.html` | Main UI. Contains all dashboard markup, styling, auth flow, `fetch` calls, local challan calculator, GPS tracker, theme toggling, and Web Speech API voice input. Backend URL is hardcoded to `http://127.0.0.1:8000`. |

`scripts/`:

| File | Role |
| --- | --- |
| `scripts/preprocess.py` | Reads raw corpus files, extracts text from PDFs/CSVs/text, chunks content, infers metadata, writes `data/processed/chunks.json`, and prints counts by region/category. |
| `scripts/migrate_to_qdrant.py` | Loads processed chunks, embeds them, recreates Qdrant collection `drivelegal_chunks`, and uploads points in batches of 100. |
| `scripts/cache_rules.py` | Loads Qdrant and runs seed queries through `get_answer()` to refresh `data/cache.json`. |
| `scripts/setup_gps.bat` | Windows helper to install `reverse_geocoder`, check Ollama at `localhost:11434`, and test Pune reverse geocoding. |

### Jurisdiction Hierarchy as Data Structures

DriveLegal now organizes Indian traffic laws and regulations using a **multi-level jurisdiction hierarchy**:

**Hierarchy Levels:**
```
National (India)
  ├── State (e.g., Maharashtra, Delhi, Karnataka)
  │    └── City (e.g., Mumbai, Bangalore, Pune)
```

**How It Works:**

1. **Metadata Tagging**: Each content chunk in the Qdrant database is tagged with its applicable jurisdiction levels (`national`, `state`, `city`, or `local`).
   - Source files are analyzed during preprocessing (e.g., `Maharashtra.pdf` → Maharashtra state-level content)
   - Chunks are automatically associated with their jurisdiction scope based on content analysis and source attribution

2. **Cascading Retrieval**: When a user asks a question from a specific location, the retriever prioritizes content in this order:
   - **City-level content** (most specific, highest priority)
   - **State-level content** (applicable to the entire state)
   - **National/country-level content** (general Indian traffic laws)
   - **All content** (fallback, if no jurisdiction-specific matches found)

3. **GPS-to-Jurisdiction Mapping**: When a user enables GPS:
   - `geocoding/offline_geocoder.py` resolves coordinates to a `LocationResult` containing structured jurisdiction data
   - The system identifies the city, state, and country from the GPS coordinates
   - This hierarchy is passed directly to the retriever for location-aware content filtering

4. **Fine Lookup Integration**: `data/fines.json` is organized by jurisdiction hierarchy:
   - National-level fine amounts
   - State-specific overrides
   - City-specific fines (highest priority)
   - `rag/challan.py` uses this hierarchy to return the most accurate fine for a violation

5. **Preprocessing Enhancement**: `scripts/preprocess.py` infers jurisdiction metadata using:
   - Keyword matching (e.g., "Maharashtra Motor Vehicle Rules" → Maharashtra state)
   - File name patterns (e.g., files named after states/cities)
   - Explicit metadata in source documents

**Benefits:**
- More accurate, location-specific legal guidance
- Reduced irrelevant search results from other jurisdictions
- Better support for India's federal traffic law structure (national + state-level regulations)
- Improved fine estimation accuracy

`data/`:

| File/folder | Role |
| --- | --- |
| `data/fines.json` | Structured fine database keyed by region, violation, and vehicle/default. Last-updated note inside file is `2024-01`. |
| `data/cache.json` | Offline Q&A fallback cache with 25 entries. |
| `data/processed/chunks.json` | Generated retrieval corpus. Inspected file contains 30,633 chunks. |
| `data/qdrant_db/meta.json` | Embedded Qdrant metadata for collection `drivelegal_chunks` with vector size 384 and cosine distance. |
| `data/qdrant_db/collection/drivelegal_chunks/storage.sqlite` | Generated embedded Qdrant storage. Inspected file contains 30,633 rows in `points`. |
| `data/raw/*.pdf` | Legal/rules/fines source PDFs used by preprocessing, including central Motor Vehicles Act/Rules and state-specific motor vehicle rules. |
| `data/raw/*.csv` | CSV source data. `Indian_Traffic_Violations.csv` has 4,000 rows; `echallan_daily_data.csv` has 4,061 rows in the inspected checkout. |
| `data/raw/mva_helmet.txt` | Small text source containing a helmet rule/fine snippet. |

`qdrant_db/`:

| File/folder | Role |
| --- | --- |
| `qdrant_db/meta.json` | Root-level Qdrant metadata file with no collections in the inspected checkout. Runtime code does not point here. |
| `qdrant_db/.lock` | Generated lock file. Runtime code does not point here. |

`tests/`:

| File | Role |
| --- | --- |
| `tests/test_api.py` | FastAPI endpoint tests using `TestClient` and mocks. Currently posts to `/chat` without a bearer token even though the route requires `HTTPBearer`. |
| `tests/test_challan.py` | Unit tests for violation/vehicle detection and fine lookup cascade. |
| `tests/test_rag.py` | Unit tests for retriever and chain behavior. Some mocks appear stale against the current `(docs, top_score)` retriever return signature. |

`logs/`:

| File | Role |
| --- | --- |
| `logs/app.log` | Runtime log written by `app/main.py`. Logs are ignored by `.gitignore`, but this file is tracked in the inspected checkout. |

## 8. Requirements / Prerequisites

Runtime:

| Requirement | Source | Notes |
| --- | --- | --- |
| Python 3.11 | `.python-version`, `runtime.txt` | Required by project metadata. |
| Ollama | `rag/chain.py`, setup docs | Must be installed and running locally for non-fallback LLM answers. |
| Ollama model `mistral` | `ChatOllama(model="mistral")` | Pull/run with Ollama before using full RAG generation. |
| SQLite | Python standard library/SQLAlchemy | File-based DB; no external database server required. |
| SMTP account | `app/auth.py` | Required for `/api/auth/send-otp` to work. Gmail app passwords are implied by `.env.example` placeholders but any compatible SMTP SSL server can be configured. |
| Internet for first setup | Dependencies/model/CDN | Needed to install Python packages, download sentence-transformer/Ollama models, and load Google Fonts/Font Awesome CDN assets unless cached. |
| `pdfplumber` | `scripts/preprocess.py` | Required for PDF preprocessing; not currently pinned in `requirements.txt`. |

Environment variables consumed by current source:

| Variable | Required? | Used by | Purpose/default |
| --- | --- | --- | --- |
| `SMTP_SERVER` | Required for OTP email unless default works | `app/auth.py` | SMTP SSL host; defaults to `smtp.gmail.com`. |
| `SMTP_PORT` | Required for OTP email unless default works | `app/auth.py` | SMTP SSL port; defaults to `465`. |
| `SENDER_EMAIL` | Yes for `/api/auth/send-otp` | `app/auth.py` | From address and SMTP login username. |
| `SENDER_PASSWORD` | Yes for `/api/auth/send-otp` | `app/auth.py` | SMTP login password/app password. |
| `JWT_SECRET` | Strongly required | `app/auth.py` | JWT signing/verification secret; code has an unsafe fallback if unset. |
| `GPS_MAX_AGE_SECONDS` | No | `app/routes/location.py` | Number of seconds before a GPS fix is stale; defaults to `60`. |
| `SUREPASS_API_BASE_URL` | TODO: confirm | `app/routes/profile.py` | Read with default `https://api.surepass.io/api/v1`, but not used in an HTTP call. |
| `SUREPASS_AUTH_TOKEN` | TODO: confirm | `app/routes/profile.py` | Read into `SUREPASS_TOKEN`, but not used in an HTTP call. |

Variables present in `.env.example` or `.env` but not consumed by current source:

| Variable | Current status |
| --- | --- |
| `HOST` | Not read by Python source or batch scripts; `start.bat` hardcodes `0.0.0.0`. |
| `PORT` | Not read by Python source or batch scripts; `start.bat` hardcodes `8000`. |
| `BACKEND_URL` | Not read by `frontend/app.py` or `frontend/ui.html`; UI hardcodes `http://127.0.0.1:8000`. |
| `LOG_LEVEL` | Not read; `app/main.py` hardcodes `logging.INFO`. |
| `GPS_POLL_INTERVAL_MS` | Present in tracked `.env`, not `.env.example`; UI hardcodes `GPS_PUSH_INTERVAL_MS = 10000`. |

## 9. Setup & Installation

Recommended manual setup from a clean clone:

```powershell
git clone <repo-url>
cd DriveLegal

python -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python -m pip install pdfplumber

copy .env.example .env
```

Edit `.env` and set at least:

```dotenv
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SENDER_EMAIL=your_email@gmail.com
SENDER_PASSWORD=your_app_password
JWT_SECRET=replace_with_a_long_random_secret
GPS_MAX_AGE_SECONDS=60
```

If live Surepass integration is later implemented, also set:

```dotenv
SUREPASS_API_BASE_URL=https://api.surepass.io/api/v1
SUREPASS_AUTH_TOKEN=your_surepass_token
```

Install and prepare Ollama:

```powershell
ollama pull mistral
ollama serve
```

If `ollama serve` reports that Ollama is already running, keep the running daemon.

Build or refresh the local retrieval index:

```powershell
python scripts\preprocess.py
python scripts\migrate_to_qdrant.py
```

Start the backend:

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Start the Streamlit UI in a second terminal:

```powershell
.\venv\Scripts\Activate.ps1
streamlit run frontend\app.py
```

Open:

| Surface | URL |
| --- | --- |
| Streamlit UI | `http://localhost:8501` |
| Direct FastAPI-served UI | `http://localhost:8000/ui` |
| API docs | `http://localhost:8000/docs` |
| Health check | `http://localhost:8000/health` |

Windows helper scripts:

```powershell
scripts\setup_gps.bat
start.bat
```

Do not rely on `setup.bat` for full setup until its missing `scripts\embed.py` reference is corrected to `scripts\migrate_to_qdrant.py`.

Run tests after installing dependencies:

```powershell
python -m pytest tests -q
```

## 10. Configuration

| Configuration | Location | Effect |
| --- | --- | --- |
| SQLite database URL | `app/database.py` | Hardcoded to `sqlite:///./drivelegal.db`; running from a different working directory changes where the DB file is created/read. |
| CORS policy | `app/main.py` | Allows all origins, methods, and headers. Tighten for production. |
| Logging | `app/main.py` | Logs to `logs/app.log` and stdout at `INFO`; `LOG_LEVEL` is not currently read. |
| Qdrant path | `rag/retriever.py`, `scripts/migrate_to_qdrant.py` | Both use `data/qdrant_db`. |
| Qdrant collection | `rag/retriever.py`, `scripts/migrate_to_qdrant.py` | `drivelegal_chunks`. |
| Embedding model | `rag/retriever.py`, `scripts/migrate_to_qdrant.py` | `all-MiniLM-L6-v2`, vector size 384. |
| LLM model | `rag/chain.py` | Hardcoded `mistral` via `ChatOllama(model="mistral", temperature=0)`. |
| Offline cache path | `rag/chain.py` | `data/cache.json`. |
| Fine data path | `rag/challan.py` | `data/fines.json` (now jurisdiction-aware by city → state → national priority). |
| Jurisdiction retrieval cascade | `rag/retriever.py` | City → State → Country → All (unfiltered), with exact-match filtering at each level. |
| GPS freshness | `app/routes/location.py` | `GPS_MAX_AGE_SECONDS`, default `60`. |
| GPS-to-jurisdiction mapping | `geocoding/offline_geocoder.py` | Resolves coordinates to structured city/state/country data for cascading retrieval. |
| Frontend API origin | `frontend/ui.html` | Hardcoded `const BACKEND = 'http://127.0.0.1:8000';`. |
| Frontend GPS push interval | `frontend/ui.html` | Hardcoded `GPS_PUSH_INTERVAL_MS = 10000`. |
| JWT expiration | `app/auth.py` | Tokens expire seven days after verification. |
| OTP expiration | `app/auth.py` | OTPs expire after five minutes. |
| Profile sample data | `app/routes/profile.py` | DL numbers containing `ANURAG` or `TEAMKALKI` select local sample profiles; otherwise a default profile/violation is used. |
| Preprocessing metadata inference | `scripts/preprocess.py` | Region/category/law section/jurisdiction hierarchy inferred with keyword and regex hints. |

## 11. Known Limitations / TODOs

- TODO: confirm live Surepass/Parivahan integration. `app/routes/profile.py` reads `SUREPASS_API_BASE_URL` and `SUREPASS_AUTH_TOKEN`, but current code never imports `requests`/`httpx` or performs a live Surepass call.
- TODO: confirm license. The previous README stated MIT, but no standalone `LICENSE` file exists.
- TODO: enhance jurisdiction hierarchy with local/micro-jurisdiction levels (e.g., police station jurisdictions, municipal corporation zones).
- TODO: optimize jurisdiction hierarchy filtering performance with Qdrant payload indexing for city/state/country metadata.
- `setup.bat` references `python scripts\embed.py`, but no `scripts/embed.py` exists. The actual Qdrant build script is `scripts/migrate_to_qdrant.py`.
- `scripts/preprocess.py` imports `pdfplumber`, but `requirements.txt` does not list it.
- `/chat` requires a bearer token through `HTTPBearer`. Current `tests/test_api.py` posts to `/chat` without an `Authorization` header, so the tests likely need updates.
- `rag/retriever.retrieve()` returns `(documents, top_score)`, but parts of `tests/test_rag.py` mock it as a bare list, so those tests appear stale.
- The checked-in `venv` is not portable and failed to run in this environment because its Python launcher points to `C:\Users\HP\AppData\Local\Programs\Python\Python311\python.exe`.
- `.env`, `logs/app.log`, `drivelegal.db`, and `app/drivelegal.db` are tracked in this checkout even though `.gitignore` says environment variables/logs and generated artifacts should not be committed. Rotate any real secrets before public distribution.
- `JWT_SECRET` has an unsafe fallback string if unset. Production should fail fast instead of signing tokens with a fallback.
- OTPs and GPS fixes are stored only in process memory. They are lost on restart and are not shared across multiple workers/processes.
- CORS is configured with `allow_origins=["*"]`, `allow_methods=["*"]`, and `allow_headers=["*"]`.
- Dashboard/profile endpoints use `user_id` path parameters and do not currently enforce that the caller's JWT belongs to that `user_id`.
- `frontend/ui.html` hardcodes the backend URL and GPS interval; `.env`/`.env.example` values do not control them.
- The UI inserts chat message text into `innerHTML` after only replacing newlines, so server-returned text is rendered as HTML.
- `frontend/app.py` imports `json`, `requests`, and `streamlit` twice; `requests` is not used in the current wrapper.
- `requirements.txt` includes `slowapi`, but no current source imports or configures it.
- `HOST`, `PORT`, `BACKEND_URL`, and `LOG_LEVEL` are documented in `.env.example` but not currently consumed by code.
- `data/fines.json` notes `_last_updated` as `2024-01`; fine amounts should be verified against official authorities before production use. Jurisdiction hierarchy in fines.json should be validated against actual state/city-level traffic authority regulations.
- `scripts/preprocess.py` strips non-ASCII text, which may remove Indian-language text from raw law PDFs.
- Region filtering in `rag/retriever.py` uses exact matches. National chunks generated by preprocessing often use region `National`, while chat passes country `India`; the final unfiltered fallback still finds national content, but the country filter stage may not match those chunks.
- No Dockerfile, Compose file, or CI/CD config exists.

## 12. License / Contributing

No standalone `LICENSE` file was found. TODO: confirm whether the project is intended to be MIT licensed, because the previous README claimed MIT.

`github guide.txt` contains a minimal collaboration workflow:

```text
git fetch upstream
git merge upstream/main
git push origin main

git add .
git commit -m "Your message"
git push origin main
```
